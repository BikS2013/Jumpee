# Task Completion Checklist

1. Build the app: `bash build.sh` — must succeed with no errors
2. Update `docs/design/project-design.md` if design changed
3. Update `Issues - Pending Items.md` if issues found/fixed
4. Update CLAUDE.md if new tools created
5. Update `docs/design/project-functions.md` if new features added
6. Do NOT perform version control operations unless explicitly asked
