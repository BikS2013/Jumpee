# Suggested Commands

## Build
```bash
cd /Users/giorgosmarinos/aiwork/coding-platform/macbook-desktop/Jumpee && bash build.sh
```

## Run
```bash
open build/Jumpee.app
```

## Install
```bash
cp -r build/Jumpee.app /Applications/
```

## Package (DMG)
```bash
bash package.sh
```

## System utilities
- `git` — version control
- `swiftc` — Swift compiler
- `codesign` — code signing
- `defaults` — macOS defaults
